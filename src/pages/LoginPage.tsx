import { Button } from "@mui/material";

function LoginPage() {
  return (
    <div>
      <h1>Login Page</h1>
      <Button variant="contained">Click me!</Button>
    </div>
  );
}

export default LoginPage;
